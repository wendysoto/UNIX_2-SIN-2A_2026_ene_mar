#cambia de directorio
cd / 
#lista directorio actual
ls
#cambia a directorio dev
cd dev
ls
#cambio a directorio bin
cd /bin
ls
#ver contenido del archivo
cat ls
file ls
# punto  es directorio actual
# dos puntos es directorio padre
cd ..
#/etc almacena las configuraciones
cd /etc
ls
#Imprime variable de entorno SHELL (el que interpreta los comandos)
echo $SHELL